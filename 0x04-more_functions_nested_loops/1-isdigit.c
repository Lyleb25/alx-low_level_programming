#include "main.h"
#include <stdio.h>

/**
 * _isdigit - Check for a digit program.
 *
 * Return: 1 if c is a digit otherwise 0
 */
int _isdigit(int c)
{
	c = 0;
	       while (c <= 9);
	       c++;
	       
		       if c = "%d"
		       {
			       return (1);
		       }
		       else 
		       
			       return (0);
		       
}
